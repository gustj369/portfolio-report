# AGENTS.md

## 작업 전 반드시 확인

- 현재 작업 위치가 맞는지 확인한다. 기본 프로젝트는 `portfolio-report`이다.
- 기존 파일 구조와 구현 패턴을 먼저 읽고, 불필요한 재구성이나 대규모 리팩터링은 피한다.
- 프론트엔드, 백엔드, 배포 설정 중 어느 영역을 바꾸는 작업인지 먼저 구분한다.
- 환경 변수, API 키, 결제 키, 저장소 키 같은 비밀 정보는 코드에 직접 넣지 않는다.
- 사용자가 명시적으로 요청하지 않은 파일 삭제, 되돌리기, 강제 초기화는 하지 않는다.
- 의존성 설치, 외부 다운로드, 배포 관련 작업은 필요성을 먼저 판단하고 진행한다.

## 프로젝트 목적

이 프로젝트는 포트폴리오 정보를 입력받아 AI 기반 자산 분석과 시뮬레이션을 수행하고, PDF 리포트를 생성하는 서비스이다.

- 프론트엔드: Next.js, React, TypeScript, Tailwind CSS
- 백엔드: FastAPI, Python
- 리포트 생성: ReportLab, matplotlib
- 데이터/API: Gemini API, yfinance, FRED API
- 저장소/배포: 로컬 파일, Cloudflare R2 또는 S3 계열 저장소, Vercel, Render

## 개발 스타일

- 빠르게 실행 가능한 결과물을 만들되, 기존 구조와 흐름을 존중한다.
- 사용자가 원하는 기능을 먼저 완성하고, 필요할 때만 정리와 개선을 더한다.
- 에러가 나면 추측으로 덮지 말고 로그, 설정, 실행 흐름을 확인한다.
- 한글 PDF와 리포트 품질이 중요하므로 폰트, 인코딩, 줄바꿈, 표/차트 렌더링을 신경 쓴다.
- 프론트엔드는 실제 사용자가 입력부터 리포트 생성까지 자연스럽게 진행할 수 있는 흐름을 우선한다.
- 백엔드는 API 응답, 예외 처리, 파일 생성, 저장소 연동이 안정적으로 이어지도록 작성한다.

## 프로젝트 규칙

- 백엔드는 `portfolio-report/backend` 기준으로 작업한다.
- 프론트엔드는 `portfolio-report/frontend` 기준으로 작업한다.
- 로컬 개발 기본 포트는 백엔드 `8000`, 프론트엔드 `3000`을 우선 사용한다.
- PDF 생성에 필요한 한글 폰트는 `backend/assets/fonts` 아래에서 관리한다.
- 생성된 리포트 파일은 개발 환경에서는 로컬 저장소를 사용할 수 있다.
- 배포 환경에서는 로컬 저장소 대신 R2/S3 계열 저장소 사용을 우선 고려한다.
- 결제 기능은 개발 모드와 실제 결제 모드를 구분해서 다룬다.
- 환경 변수 예시는 문서화하되 실제 키 값은 커밋하지 않는다.

## 수정 전 체크리스트

- 요청한 변경 범위를 한 문장으로 정리했는가?
- 관련 파일을 먼저 읽고 현재 구현 방식을 확인했는가?
- 같은 기능을 이미 처리하는 유틸, 서비스, 컴포넌트가 있는가?
- 변경이 프론트엔드, 백엔드, PDF, 배포 설정 중 어디에 영향을 주는가?
- 새 의존성이 정말 필요한가?
- 비밀 정보나 로컬 경로를 코드에 고정하지 않았는가?

## 수정 후 체크리스트

- 변경한 기능이 요청 의도와 맞는가?
- 타입 오류, 린트 오류, 실행 오류 가능성을 확인했는가?
- 가능한 경우 관련 테스트나 빌드, 최소 실행 검증을 했다.
- PDF/한글/차트 관련 변경이면 출력 품질을 확인했다.
- API 변경이면 프론트엔드 호출부와 응답 형식도 함께 확인했다.
- 배포 설정 변경이면 필요한 환경 변수를 문서나 설정에 반영했다.
- 사용자가 건드린 것으로 보이는 unrelated 변경은 되돌리지 않았다.

## 코딩 에이전트 지침

- 작업을 시작하면 먼저 파일을 읽고 현재 상태를 파악한다.
- 코드 수정은 작고 명확한 단위로 진행한다.
- 기존 명명 규칙, 폴더 구조, 스타일을 따른다.
- 단순 문자열 조작보다 가능한 한 타입, 모델, 설정 객체, 라이브러리 API를 사용한다.
- 외부 명령은 목적이 분명할 때만 실행하고, 결과를 작업 판단에 반영한다.
- 설치나 다운로드가 필요한 경우 프로젝트에 이미 있는 방식이 있는지 먼저 확인한다.
- Git 작업은 조심스럽게 수행하며, 사용자가 요청하지 않은 커밋, 푸시, 되돌리기는 하지 않는다.

## Claude Code 전용 표현 변환 기준

- `Bash(...)` 허용 목록은 “필요한 터미널 명령을 목적에 맞게 실행한다”로 해석한다.
- `PowerShell(...)` 허용 목록은 Windows 환경에서 필요한 셸 작업을 할 수 있다는 뜻으로 해석한다.
- `Skill(claude-api)` 같은 Claude 전용 기능은 일반적인 AI/API 연동 지침으로 바꿔 해석한다.
- 특정 도구 문법보다 프로젝트의 실제 의도, 실행 흐름, 안전 규칙을 우선한다.

