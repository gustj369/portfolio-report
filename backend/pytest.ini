[pytest]
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
filterwarnings =
    # google-genai 1.x가 Python 3.14 내부 typing API(_UnionGenericAlias)를 사용해 발생하는
    # DeprecationWarning — Python 3.17 제거 예정이나 현재 동작에는 영향 없음
    ignore::DeprecationWarning:google.genai
